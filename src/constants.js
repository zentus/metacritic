const BASE_URL = 'https://www.metacritic.com'
const NEW_SERIES_URL = `${BASE_URL}/browse/tv/release-date/new-series/date`
const NEW_SEASONS_URL = `${BASE_URL}/browse/tv/release-date/returning-series/date`

module.exports = {
  BASE_URL,
  NEW_SEASONS_URL,
  NEW_SERIES_URL
}
